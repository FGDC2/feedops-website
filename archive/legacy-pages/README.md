# Legacy Pages Archive

These pages were moved out of `site/` so they remain in git without being deployed.

Use this archive only when intentionally referencing old imported pages. Current public pages live in `site/` and are the only pages included in the Render build.
