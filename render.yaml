services:
  - type: web
    name: feedops-website-static
    runtime: static
    buildCommand: node scripts/build-render-site.mjs
    staticPublishPath: render-site
    autoDeployTrigger: commit
    renderSubdomainPolicy: disabled
